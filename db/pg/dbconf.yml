production:
    driver: $RTFBLOG_DB_DRIVER
    dsn: $RTFBLOG_DB_URL
    migrationsDir: .

development:
    driver: $RTFBLOG_DB_DRIVER
    dsn: $RTFBLOG_DB_TEST_URL
    migrationsDir: .

staging:
    driver: $RTFBLOG_DB_DRIVER
    dsn: $RTFBLOG_DB_STAGING_TEST_URL
    migrationsDir: .
